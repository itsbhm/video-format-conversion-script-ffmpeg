# Ignore system files
.DS_Store

# Ignore the input video file
input.mp4

# Ignore all files inside the output directory
output/*
# But do not ignore the output directory itself
!output/

# Allow .gitkeep files to be tracked (if included)
!output/.gitkeep
